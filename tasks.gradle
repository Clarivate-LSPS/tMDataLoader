task deployJar(dependsOn: jar) {
    ext.outputFile = new File(rootDir, 'tm_etl.jar')
    inputs.file jar.archivePath
    outputs.files outputFile

    doLast {
        ant.copy(tofile: outputFile, file: jar.archivePath)
    }
}